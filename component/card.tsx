import React from "react"  ;

interface CardProps {
    route: string;
    plateNumber: string;
    currentMaxload: number;
    currentLoad : number;   
    maxLoad: number;
    status: string;
    
}